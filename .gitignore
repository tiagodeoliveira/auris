# Rust
/target
**/target

# Node
node_modules
dist
.pnpm-store
*.tsbuildinfo

# Swift / Xcode
.build
.swiftpm
DerivedData
xcuserdata
*.xcworkspace/xcuserdata

# Editor
.vscode
.idea
*.swp
.DS_Store

# Env
.env
.env.local

# Server data dir (sqlite + future blobs). Default location used by
# `just server-run`; production / docker mount their own path via
# MEETING_COMPANION_DATA_DIR.
/data
**/data/server.db
**/data/server.db-journal
**/data/server.db-wal
**/data/server.db-shm
**/data/blobs/

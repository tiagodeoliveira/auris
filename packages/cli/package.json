{
  "name": "@auris/cli",
  "version": "0.23.0",
  "private": true,
  "type": "module",
  "description": "Auris CLI (auris) and MCP server (auris-mcp) for read-only access to your meetings.",
  "bin": {
    "auris": "./dist/cli/index.js",
    "auris-mcp": "./dist/mcp/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/mcp/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^15.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.6"
  }
}

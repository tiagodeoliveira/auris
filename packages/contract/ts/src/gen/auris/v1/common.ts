// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: auris/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "auris.v1";

/** Lifecycle state of a single user's meeting. */
export enum MeetingState {
  MEETING_STATE_UNSPECIFIED = 0,
  MEETING_STATE_IDLE = 1,
  MEETING_STATE_ACTIVE = 2,
  MEETING_STATE_PAUSED = 3,
  UNRECOGNIZED = -1,
}

export function meetingStateFromJSON(object: any): MeetingState {
  switch (object) {
    case 0:
    case "MEETING_STATE_UNSPECIFIED":
      return MeetingState.MEETING_STATE_UNSPECIFIED;
    case 1:
    case "MEETING_STATE_IDLE":
      return MeetingState.MEETING_STATE_IDLE;
    case 2:
    case "MEETING_STATE_ACTIVE":
      return MeetingState.MEETING_STATE_ACTIVE;
    case 3:
    case "MEETING_STATE_PAUSED":
      return MeetingState.MEETING_STATE_PAUSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MeetingState.UNRECOGNIZED;
  }
}

export function meetingStateToJSON(object: MeetingState): string {
  switch (object) {
    case MeetingState.MEETING_STATE_UNSPECIFIED:
      return "MEETING_STATE_UNSPECIFIED";
    case MeetingState.MEETING_STATE_IDLE:
      return "MEETING_STATE_IDLE";
    case MeetingState.MEETING_STATE_ACTIVE:
      return "MEETING_STATE_ACTIVE";
    case MeetingState.MEETING_STATE_PAUSED:
      return "MEETING_STATE_PAUSED";
    case MeetingState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * How a mode's `items` list is updated when the server emits an
 * `ItemsUpdate`. `replace` swaps the whole list; `append` adds new
 * items at the tail.
 */
export enum UpdateStrategy {
  UPDATE_STRATEGY_UNSPECIFIED = 0,
  UPDATE_STRATEGY_REPLACE = 1,
  UPDATE_STRATEGY_APPEND = 2,
  UNRECOGNIZED = -1,
}

export function updateStrategyFromJSON(object: any): UpdateStrategy {
  switch (object) {
    case 0:
    case "UPDATE_STRATEGY_UNSPECIFIED":
      return UpdateStrategy.UPDATE_STRATEGY_UNSPECIFIED;
    case 1:
    case "UPDATE_STRATEGY_REPLACE":
      return UpdateStrategy.UPDATE_STRATEGY_REPLACE;
    case 2:
    case "UPDATE_STRATEGY_APPEND":
      return UpdateStrategy.UPDATE_STRATEGY_APPEND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UpdateStrategy.UNRECOGNIZED;
  }
}

export function updateStrategyToJSON(object: UpdateStrategy): string {
  switch (object) {
    case UpdateStrategy.UPDATE_STRATEGY_UNSPECIFIED:
      return "UPDATE_STRATEGY_UNSPECIFIED";
    case UpdateStrategy.UPDATE_STRATEGY_REPLACE:
      return "UPDATE_STRATEGY_REPLACE";
    case UpdateStrategy.UPDATE_STRATEGY_APPEND:
      return "UPDATE_STRATEGY_APPEND";
    case UpdateStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Capabilities a connected client offers to the meeting. Drives the
 * audio-source picker (filters by `audio_capture`), the
 * moment-screenshot trigger (filters by `screen_capture`), etc.
 */
export enum Capability {
  CAPABILITY_UNSPECIFIED = 0,
  CAPABILITY_AUDIO_CAPTURE = 1,
  CAPABILITY_SCREEN_CAPTURE = 2,
  CAPABILITY_CONTROL_SURFACE = 3,
  /**
   * CAPABILITY_SYSTEM_AUDIO - Sub-capability of audio_capture: distinguishes "Mac with
   * ScreenCaptureKit system audio" from "phone with mic permission."
   */
  CAPABILITY_SYSTEM_AUDIO = 4,
  UNRECOGNIZED = -1,
}

export function capabilityFromJSON(object: any): Capability {
  switch (object) {
    case 0:
    case "CAPABILITY_UNSPECIFIED":
      return Capability.CAPABILITY_UNSPECIFIED;
    case 1:
    case "CAPABILITY_AUDIO_CAPTURE":
      return Capability.CAPABILITY_AUDIO_CAPTURE;
    case 2:
    case "CAPABILITY_SCREEN_CAPTURE":
      return Capability.CAPABILITY_SCREEN_CAPTURE;
    case 3:
    case "CAPABILITY_CONTROL_SURFACE":
      return Capability.CAPABILITY_CONTROL_SURFACE;
    case 4:
    case "CAPABILITY_SYSTEM_AUDIO":
      return Capability.CAPABILITY_SYSTEM_AUDIO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Capability.UNRECOGNIZED;
  }
}

export function capabilityToJSON(object: Capability): string {
  switch (object) {
    case Capability.CAPABILITY_UNSPECIFIED:
      return "CAPABILITY_UNSPECIFIED";
    case Capability.CAPABILITY_AUDIO_CAPTURE:
      return "CAPABILITY_AUDIO_CAPTURE";
    case Capability.CAPABILITY_SCREEN_CAPTURE:
      return "CAPABILITY_SCREEN_CAPTURE";
    case Capability.CAPABILITY_CONTROL_SURFACE:
      return "CAPABILITY_CONTROL_SURFACE";
    case Capability.CAPABILITY_SYSTEM_AUDIO:
      return "CAPABILITY_SYSTEM_AUDIO";
    case Capability.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * One mode (tab) the user can switch to within an active meeting.
 * The server defines the catalog at compile time; clients render
 * whatever the snapshot delivers (no client-side hardcoding).
 */
export interface ModeOption {
  id: string;
  label: string;
  updateStrategy: UpdateStrategy;
}

/**
 * One row in a mode's items list. `meta` is mode-specific extras
 * (e.g. speaker tag for transcript items, importance for highlights,
 * role for chat bubbles).
 */
export interface Item {
  id: string;
  text: string;
  /** Lazy-loaded long-form text; empty until `expand_item` populates it. */
  detail?: string | undefined;
  /** Milliseconds since the meeting started. */
  t: number;
  /**
   * Mode-specific extras. Heterogeneous JSON-like structure;
   * protobuf's google.protobuf.Struct is the right shape but
   * requires importing it. Modeled as a serialized JSON string for
   * wire stability — clients parse on demand. (Future: switch to
   * google.protobuf.Struct if the meta surface stabilizes.)
   */
  metaJson?: string | undefined;
}

/**
 * Heartbeat-driven status the server pushes ~every 3s during an
 * active meeting. `error` carries transient error strings (e.g.
 * LLM extraction failed) that surface as toasts in the UI.
 */
export interface Status {
  listening: boolean;
  paused: boolean;
  error?: string | undefined;
}

/**
 * Counts of memories recalled from mnemo at meeting start. Drives
 * the "★ memory · N recalled" badge on PWA / Mac / mobile.
 */
export interface PriorContextSummary {
  preferences: number;
  facts: number;
  episodes: number;
  projectMemories: number;
}

/**
 * A registered client device. The server tracks one entry per active
 * WS connection that has sent `RegisterDevice`. `online` flips to
 * false on disconnect.
 */
export interface Device {
  id: string;
  hostname: string;
  capabilities: Capability[];
  online: boolean;
}

function createBaseModeOption(): ModeOption {
  return { id: "", label: "", updateStrategy: 0 };
}

export const ModeOption: MessageFns<ModeOption> = {
  encode(message: ModeOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    if (message.updateStrategy !== 0) {
      writer.uint32(24).int32(message.updateStrategy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModeOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModeOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.updateStrategy = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModeOption {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      updateStrategy: isSet(object.updateStrategy)
        ? updateStrategyFromJSON(object.updateStrategy)
        : isSet(object.update_strategy)
          ? updateStrategyFromJSON(object.update_strategy)
          : 0,
    };
  },

  toJSON(message: ModeOption): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.updateStrategy !== 0) {
      obj.updateStrategy = updateStrategyToJSON(message.updateStrategy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModeOption>, I>>(base?: I): ModeOption {
    return ModeOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModeOption>, I>>(object: I): ModeOption {
    const message = createBaseModeOption();
    message.id = object.id ?? "";
    message.label = object.label ?? "";
    message.updateStrategy = object.updateStrategy ?? 0;
    return message;
  },
};

function createBaseItem(): Item {
  return { id: "", text: "", detail: undefined, t: 0, metaJson: undefined };
}

export const Item: MessageFns<Item> = {
  encode(message: Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.detail !== undefined) {
      writer.uint32(26).string(message.detail);
    }
    if (message.t !== 0) {
      writer.uint32(32).uint64(message.t);
    }
    if (message.metaJson !== undefined) {
      writer.uint32(42).string(message.metaJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.t = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.metaJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Item {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      detail: isSet(object.detail) ? globalThis.String(object.detail) : undefined,
      t: isSet(object.t) ? globalThis.Number(object.t) : 0,
      metaJson: isSet(object.metaJson)
        ? globalThis.String(object.metaJson)
        : isSet(object.meta_json)
          ? globalThis.String(object.meta_json)
          : undefined,
    };
  },

  toJSON(message: Item): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.detail !== undefined) {
      obj.detail = message.detail;
    }
    if (message.t !== 0) {
      obj.t = Math.round(message.t);
    }
    if (message.metaJson !== undefined) {
      obj.metaJson = message.metaJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Item>, I>>(base?: I): Item {
    return Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Item>, I>>(object: I): Item {
    const message = createBaseItem();
    message.id = object.id ?? "";
    message.text = object.text ?? "";
    message.detail = object.detail ?? undefined;
    message.t = object.t ?? 0;
    message.metaJson = object.metaJson ?? undefined;
    return message;
  },
};

function createBaseStatus(): Status {
  return { listening: false, paused: false, error: undefined };
}

export const Status: MessageFns<Status> = {
  encode(message: Status, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.listening !== false) {
      writer.uint32(8).bool(message.listening);
    }
    if (message.paused !== false) {
      writer.uint32(16).bool(message.paused);
    }
    if (message.error !== undefined) {
      writer.uint32(26).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Status {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.listening = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.paused = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Status {
    return {
      listening: isSet(object.listening) ? globalThis.Boolean(object.listening) : false,
      paused: isSet(object.paused) ? globalThis.Boolean(object.paused) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: Status): unknown {
    const obj: any = {};
    if (message.listening !== false) {
      obj.listening = message.listening;
    }
    if (message.paused !== false) {
      obj.paused = message.paused;
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Status>, I>>(base?: I): Status {
    return Status.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Status>, I>>(object: I): Status {
    const message = createBaseStatus();
    message.listening = object.listening ?? false;
    message.paused = object.paused ?? false;
    message.error = object.error ?? undefined;
    return message;
  },
};

function createBasePriorContextSummary(): PriorContextSummary {
  return { preferences: 0, facts: 0, episodes: 0, projectMemories: 0 };
}

export const PriorContextSummary: MessageFns<PriorContextSummary> = {
  encode(message: PriorContextSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.preferences !== 0) {
      writer.uint32(8).uint64(message.preferences);
    }
    if (message.facts !== 0) {
      writer.uint32(16).uint64(message.facts);
    }
    if (message.episodes !== 0) {
      writer.uint32(24).uint64(message.episodes);
    }
    if (message.projectMemories !== 0) {
      writer.uint32(32).uint64(message.projectMemories);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PriorContextSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePriorContextSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.preferences = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.facts = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.episodes = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.projectMemories = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PriorContextSummary {
    return {
      preferences: isSet(object.preferences) ? globalThis.Number(object.preferences) : 0,
      facts: isSet(object.facts) ? globalThis.Number(object.facts) : 0,
      episodes: isSet(object.episodes) ? globalThis.Number(object.episodes) : 0,
      projectMemories: isSet(object.projectMemories)
        ? globalThis.Number(object.projectMemories)
        : isSet(object.project_memories)
          ? globalThis.Number(object.project_memories)
          : 0,
    };
  },

  toJSON(message: PriorContextSummary): unknown {
    const obj: any = {};
    if (message.preferences !== 0) {
      obj.preferences = Math.round(message.preferences);
    }
    if (message.facts !== 0) {
      obj.facts = Math.round(message.facts);
    }
    if (message.episodes !== 0) {
      obj.episodes = Math.round(message.episodes);
    }
    if (message.projectMemories !== 0) {
      obj.projectMemories = Math.round(message.projectMemories);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PriorContextSummary>, I>>(base?: I): PriorContextSummary {
    return PriorContextSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PriorContextSummary>, I>>(
    object: I,
  ): PriorContextSummary {
    const message = createBasePriorContextSummary();
    message.preferences = object.preferences ?? 0;
    message.facts = object.facts ?? 0;
    message.episodes = object.episodes ?? 0;
    message.projectMemories = object.projectMemories ?? 0;
    return message;
  },
};

function createBaseDevice(): Device {
  return { id: "", hostname: "", capabilities: [], online: false };
}

export const Device: MessageFns<Device> = {
  encode(message: Device, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.hostname !== "") {
      writer.uint32(18).string(message.hostname);
    }
    writer.uint32(26).fork();
    for (const v of message.capabilities) {
      writer.int32(v);
    }
    writer.join();
    if (message.online !== false) {
      writer.uint32(32).bool(message.online);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Device {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.capabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.capabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Device {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => capabilityFromJSON(e))
        : [],
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
    };
  },

  toJSON(message: Device): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => capabilityToJSON(e));
    }
    if (message.online !== false) {
      obj.online = message.online;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Device>, I>>(base?: I): Device {
    return Device.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Device>, I>>(object: I): Device {
    const message = createBaseDevice();
    message.id = object.id ?? "";
    message.hostname = object.hostname ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.online = object.online ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends { $case: string }
        ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

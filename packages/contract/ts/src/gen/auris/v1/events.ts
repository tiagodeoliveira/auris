// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: auris/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  Device,
  Item,
  MeetingState,
  meetingStateFromJSON,
  meetingStateToJSON,
  ModeOption,
  PriorContextSummary,
  Status,
} from "./common";

export const protobufPackage = "auris.v1";

/**
 * First message after a successful WS connect. Carries the full
 * per-user state needed to render any client without prior context.
 */
export interface Snapshot {
  protocolVersion: number;
  meetingState: MeetingState;
  /** Server-assigned id of the active meeting. Empty when idle. */
  meetingId?: string | undefined;
  availableModes: ModeOption[];
  mode: string;
  displayTag?: string | undefined;
  metadata: { [key: string]: string };
  items: Item[];
  status?:
    | Status
    | undefined;
  /** mnemo recall counts. Absent when no recall has populated yet. */
  priorContext?:
    | PriorContextSummary
    | undefined;
  /** All currently-registered devices. Empty list = no devices yet. */
  devices: Device[];
  /**
   * Device feeding audio for the active meeting. Empty when no
   * audio source is bound.
   */
  audioSourceDeviceId?: string | undefined;
}

export interface Snapshot_MetadataEntry {
  key: string;
  value: string;
}

/**
 * After every lifecycle transition (Idle ↔ Active).
 * `meeting_id` present when going to Active; empty going Idle.
 */
export interface MeetingStateChanged {
  meetingState: MeetingState;
  meetingId?: string | undefined;
}

/**
 * After `set_mode`. Carries the items list for the new mode so the
 * client doesn't need a follow-up round trip.
 */
export interface ModeChanged {
  mode: string;
  displayTag?: string | undefined;
  items: Item[];
}

/**
 * Mode-specific status string that some modes display alongside
 * items. Empty `tag` clears the prior value.
 */
export interface DisplayTagChanged {
  tag?: string | undefined;
}

/**
 * Full metadata after any change. Send the full map, not a diff —
 * avoids sequencing bugs on lossy reconnect.
 */
export interface MetadataChanged {
  metadata: { [key: string]: string };
}

export interface MetadataChanged_MetadataEntry {
  key: string;
  value: string;
}

/**
 * After mnemo recall completes (or clears). All-zero counts means
 * cleared.
 */
export interface PriorContextChanged {
  summary?: PriorContextSummary | undefined;
}

/**
 * Per-mode item list update. Strategy depends on the mode (Replace
 * for highlights/summary/chat, Append for transcript/actions/
 * open_questions). Clients learn the mode's strategy via
 * `available_modes` in the snapshot.
 */
export interface ItemsUpdate {
  mode: string;
  items: Item[];
}

/**
 * One item changed in-place (today's only producer is `expand_item`
 * — agent writes the LLM expansion into the item's `detail`). Clients
 * replace by id within the mode's list. Works uniformly across
 * Replace and Append strategies — semantics is "replace this row,"
 * not "append" or "overwrite the whole list."
 */
export interface ItemUpdated {
  mode: string;
  item?: Item | undefined;
}

/**
 * Live in-flight transcript chunk between sentence boundaries.
 * Clients render as the dim italic "live row" in transcript mode.
 */
export interface TranscriptInterim {
  text: string;
}

/**
 * Heartbeat-driven status (~3s while active). Also used for transient
 * extraction errors.
 */
export interface StatusEvent {
  status?: Status | undefined;
}

/**
 * Per-intent rejection delivered to the originating client only.
 * Codes: `bad_payload`, `unknown_intent`, `unknown_mode`,
 * `unknown_item` (legacy `bad_json` no longer applies — payloads
 * are protobuf, not JSON).
 */
export interface Error {
  code: string;
  message: string;
  intentRef?: string | undefined;
}

/**
 * Sent only to the connection that just registered. Carries the
 * server-assigned `device_id` so the client can identify itself in
 * subsequent device lists / commands.
 */
export interface DeviceRegistered {
  device?: Device | undefined;
}

/**
 * Broadcast on registration / disconnect / capability change. Full
 * current list — clients replace their cache.
 */
export interface DevicesChanged {
  devices: Device[];
}

/**
 * Audio-source binding for the active meeting changed (start / stop
 * / rebind). Empty `device_id` means unbound.
 */
export interface AudioSourceDeviceChanged {
  deviceId?: string | undefined;
}

/**
 * Attached-artifact set for the active meeting changed (attach or
 * detach). Carries the full current set of artifact IDs so clients
 * overwrite their local mirror — no diff/incremental logic needed.
 */
export interface ArtifactsChanged {
  artifactIds: string[];
}

/**
 * Point-to-point to the bound `screen_capture`-capable device only.
 * Recipient uploads via `POST /meetings/:id/moments/:mid/screenshot`.
 */
export interface CaptureMomentScreenshot {
  meetingId: string;
  momentId: string;
  tMs: number;
}

/**
 * Moment-summary worker wrote the final summary text. Currently
 * consumed by the mnemo pusher; PWA / Mac / mobile ignore.
 */
export interface MomentSummarized {
  momentId: string;
  meetingId: string;
  tMs: number;
  summary: string;
  note?: string | undefined;
}

/** Top-level event envelope. */
export interface Event {
  kind?:
    | { $case: "snapshot"; snapshot: Snapshot }
    | { $case: "meetingStateChanged"; meetingStateChanged: MeetingStateChanged }
    | { $case: "modeChanged"; modeChanged: ModeChanged }
    | { $case: "displayTagChanged"; displayTagChanged: DisplayTagChanged }
    | { $case: "metadataChanged"; metadataChanged: MetadataChanged }
    | { $case: "priorContextChanged"; priorContextChanged: PriorContextChanged }
    | { $case: "itemsUpdate"; itemsUpdate: ItemsUpdate }
    | { $case: "itemUpdated"; itemUpdated: ItemUpdated }
    | { $case: "transcriptInterim"; transcriptInterim: TranscriptInterim }
    | { $case: "status"; status: StatusEvent }
    | { $case: "error"; error: Error }
    | { $case: "deviceRegistered"; deviceRegistered: DeviceRegistered }
    | { $case: "devicesChanged"; devicesChanged: DevicesChanged }
    | { $case: "audioSourceDeviceChanged"; audioSourceDeviceChanged: AudioSourceDeviceChanged }
    | { $case: "artifactsChanged"; artifactsChanged: ArtifactsChanged }
    | { $case: "captureMomentScreenshot"; captureMomentScreenshot: CaptureMomentScreenshot }
    | { $case: "momentSummarized"; momentSummarized: MomentSummarized }
    | undefined;
}

function createBaseSnapshot(): Snapshot {
  return {
    protocolVersion: 0,
    meetingState: 0,
    meetingId: undefined,
    availableModes: [],
    mode: "",
    displayTag: undefined,
    metadata: {},
    items: [],
    status: undefined,
    priorContext: undefined,
    devices: [],
    audioSourceDeviceId: undefined,
  };
}

export const Snapshot: MessageFns<Snapshot> = {
  encode(message: Snapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.meetingState !== 0) {
      writer.uint32(16).int32(message.meetingState);
    }
    if (message.meetingId !== undefined) {
      writer.uint32(26).string(message.meetingId);
    }
    for (const v of message.availableModes) {
      ModeOption.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.mode !== "") {
      writer.uint32(42).string(message.mode);
    }
    if (message.displayTag !== undefined) {
      writer.uint32(50).string(message.displayTag);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Snapshot_MetadataEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    for (const v of message.items) {
      Item.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.status !== undefined) {
      Status.encode(message.status, writer.uint32(74).fork()).join();
    }
    if (message.priorContext !== undefined) {
      PriorContextSummary.encode(message.priorContext, writer.uint32(82).fork()).join();
    }
    for (const v of message.devices) {
      Device.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.audioSourceDeviceId !== undefined) {
      writer.uint32(98).string(message.audioSourceDeviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Snapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.meetingState = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.meetingId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.availableModes.push(ModeOption.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.displayTag = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = Snapshot_MetadataEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.metadata[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.items.push(Item.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.status = Status.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.priorContext = PriorContextSummary.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.devices.push(Device.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.audioSourceDeviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Snapshot {
    return {
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      meetingState: isSet(object.meetingState)
        ? meetingStateFromJSON(object.meetingState)
        : isSet(object.meeting_state)
        ? meetingStateFromJSON(object.meeting_state)
        : 0,
      meetingId: isSet(object.meetingId)
        ? globalThis.String(object.meetingId)
        : isSet(object.meeting_id)
        ? globalThis.String(object.meeting_id)
        : undefined,
      availableModes: globalThis.Array.isArray(object?.availableModes)
        ? object.availableModes.map((e: any) => ModeOption.fromJSON(e))
        : globalThis.Array.isArray(object?.available_modes)
        ? object.available_modes.map((e: any) => ModeOption.fromJSON(e))
        : [],
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      displayTag: isSet(object.displayTag)
        ? globalThis.String(object.displayTag)
        : isSet(object.display_tag)
        ? globalThis.String(object.display_tag)
        : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => Item.fromJSON(e))
        : [],
      status: isSet(object.status) ? Status.fromJSON(object.status) : undefined,
      priorContext: isSet(object.priorContext)
        ? PriorContextSummary.fromJSON(object.priorContext)
        : isSet(object.prior_context)
        ? PriorContextSummary.fromJSON(object.prior_context)
        : undefined,
      devices: globalThis.Array.isArray(object?.devices)
        ? object.devices.map((e: any) => Device.fromJSON(e))
        : [],
      audioSourceDeviceId: isSet(object.audioSourceDeviceId)
        ? globalThis.String(object.audioSourceDeviceId)
        : isSet(object.audio_source_device_id)
        ? globalThis.String(object.audio_source_device_id)
        : undefined,
    };
  },

  toJSON(message: Snapshot): unknown {
    const obj: any = {};
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.meetingState !== 0) {
      obj.meetingState = meetingStateToJSON(message.meetingState);
    }
    if (message.meetingId !== undefined) {
      obj.meetingId = message.meetingId;
    }
    if (message.availableModes?.length) {
      obj.availableModes = message.availableModes.map((e) => ModeOption.toJSON(e));
    }
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.displayTag !== undefined) {
      obj.displayTag = message.displayTag;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Item.toJSON(e));
    }
    if (message.status !== undefined) {
      obj.status = Status.toJSON(message.status);
    }
    if (message.priorContext !== undefined) {
      obj.priorContext = PriorContextSummary.toJSON(message.priorContext);
    }
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => Device.toJSON(e));
    }
    if (message.audioSourceDeviceId !== undefined) {
      obj.audioSourceDeviceId = message.audioSourceDeviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Snapshot>, I>>(base?: I): Snapshot {
    return Snapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Snapshot>, I>>(object: I): Snapshot {
    const message = createBaseSnapshot();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.meetingState = object.meetingState ?? 0;
    message.meetingId = object.meetingId ?? undefined;
    message.availableModes = object.availableModes?.map((e) => ModeOption.fromPartial(e)) || [];
    message.mode = object.mode ?? "";
    message.displayTag = object.displayTag ?? undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.items = object.items?.map((e) => Item.fromPartial(e)) || [];
    message.status = (object.status !== undefined && object.status !== null)
      ? Status.fromPartial(object.status)
      : undefined;
    message.priorContext = (object.priorContext !== undefined && object.priorContext !== null)
      ? PriorContextSummary.fromPartial(object.priorContext)
      : undefined;
    message.devices = object.devices?.map((e) => Device.fromPartial(e)) || [];
    message.audioSourceDeviceId = object.audioSourceDeviceId ?? undefined;
    return message;
  },
};

function createBaseSnapshot_MetadataEntry(): Snapshot_MetadataEntry {
  return { key: "", value: "" };
}

export const Snapshot_MetadataEntry: MessageFns<Snapshot_MetadataEntry> = {
  encode(message: Snapshot_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Snapshot_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSnapshot_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Snapshot_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Snapshot_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Snapshot_MetadataEntry>, I>>(base?: I): Snapshot_MetadataEntry {
    return Snapshot_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Snapshot_MetadataEntry>, I>>(object: I): Snapshot_MetadataEntry {
    const message = createBaseSnapshot_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMeetingStateChanged(): MeetingStateChanged {
  return { meetingState: 0, meetingId: undefined };
}

export const MeetingStateChanged: MessageFns<MeetingStateChanged> = {
  encode(message: MeetingStateChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meetingState !== 0) {
      writer.uint32(8).int32(message.meetingState);
    }
    if (message.meetingId !== undefined) {
      writer.uint32(18).string(message.meetingId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeetingStateChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeetingStateChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.meetingState = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meetingId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeetingStateChanged {
    return {
      meetingState: isSet(object.meetingState)
        ? meetingStateFromJSON(object.meetingState)
        : isSet(object.meeting_state)
        ? meetingStateFromJSON(object.meeting_state)
        : 0,
      meetingId: isSet(object.meetingId)
        ? globalThis.String(object.meetingId)
        : isSet(object.meeting_id)
        ? globalThis.String(object.meeting_id)
        : undefined,
    };
  },

  toJSON(message: MeetingStateChanged): unknown {
    const obj: any = {};
    if (message.meetingState !== 0) {
      obj.meetingState = meetingStateToJSON(message.meetingState);
    }
    if (message.meetingId !== undefined) {
      obj.meetingId = message.meetingId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeetingStateChanged>, I>>(base?: I): MeetingStateChanged {
    return MeetingStateChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeetingStateChanged>, I>>(object: I): MeetingStateChanged {
    const message = createBaseMeetingStateChanged();
    message.meetingState = object.meetingState ?? 0;
    message.meetingId = object.meetingId ?? undefined;
    return message;
  },
};

function createBaseModeChanged(): ModeChanged {
  return { mode: "", displayTag: undefined, items: [] };
}

export const ModeChanged: MessageFns<ModeChanged> = {
  encode(message: ModeChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== "") {
      writer.uint32(10).string(message.mode);
    }
    if (message.displayTag !== undefined) {
      writer.uint32(18).string(message.displayTag);
    }
    for (const v of message.items) {
      Item.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModeChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModeChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayTag = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.items.push(Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModeChanged {
    return {
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      displayTag: isSet(object.displayTag)
        ? globalThis.String(object.displayTag)
        : isSet(object.display_tag)
        ? globalThis.String(object.display_tag)
        : undefined,
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: ModeChanged): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.displayTag !== undefined) {
      obj.displayTag = message.displayTag;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModeChanged>, I>>(base?: I): ModeChanged {
    return ModeChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModeChanged>, I>>(object: I): ModeChanged {
    const message = createBaseModeChanged();
    message.mode = object.mode ?? "";
    message.displayTag = object.displayTag ?? undefined;
    message.items = object.items?.map((e) => Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDisplayTagChanged(): DisplayTagChanged {
  return { tag: undefined };
}

export const DisplayTagChanged: MessageFns<DisplayTagChanged> = {
  encode(message: DisplayTagChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tag !== undefined) {
      writer.uint32(10).string(message.tag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisplayTagChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplayTagChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisplayTagChanged {
    return { tag: isSet(object.tag) ? globalThis.String(object.tag) : undefined };
  },

  toJSON(message: DisplayTagChanged): unknown {
    const obj: any = {};
    if (message.tag !== undefined) {
      obj.tag = message.tag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisplayTagChanged>, I>>(base?: I): DisplayTagChanged {
    return DisplayTagChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisplayTagChanged>, I>>(object: I): DisplayTagChanged {
    const message = createBaseDisplayTagChanged();
    message.tag = object.tag ?? undefined;
    return message;
  },
};

function createBaseMetadataChanged(): MetadataChanged {
  return { metadata: {} };
}

export const MetadataChanged: MessageFns<MetadataChanged> = {
  encode(message: MetadataChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      MetadataChanged_MetadataEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetadataChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadataChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = MetadataChanged_MetadataEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.metadata[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetadataChanged {
    return {
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: MetadataChanged): unknown {
    const obj: any = {};
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetadataChanged>, I>>(base?: I): MetadataChanged {
    return MetadataChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetadataChanged>, I>>(object: I): MetadataChanged {
    const message = createBaseMetadataChanged();
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseMetadataChanged_MetadataEntry(): MetadataChanged_MetadataEntry {
  return { key: "", value: "" };
}

export const MetadataChanged_MetadataEntry: MessageFns<MetadataChanged_MetadataEntry> = {
  encode(message: MetadataChanged_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetadataChanged_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadataChanged_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetadataChanged_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MetadataChanged_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetadataChanged_MetadataEntry>, I>>(base?: I): MetadataChanged_MetadataEntry {
    return MetadataChanged_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetadataChanged_MetadataEntry>, I>>(
    object: I,
  ): MetadataChanged_MetadataEntry {
    const message = createBaseMetadataChanged_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePriorContextChanged(): PriorContextChanged {
  return { summary: undefined };
}

export const PriorContextChanged: MessageFns<PriorContextChanged> = {
  encode(message: PriorContextChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.summary !== undefined) {
      PriorContextSummary.encode(message.summary, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PriorContextChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePriorContextChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.summary = PriorContextSummary.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PriorContextChanged {
    return { summary: isSet(object.summary) ? PriorContextSummary.fromJSON(object.summary) : undefined };
  },

  toJSON(message: PriorContextChanged): unknown {
    const obj: any = {};
    if (message.summary !== undefined) {
      obj.summary = PriorContextSummary.toJSON(message.summary);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PriorContextChanged>, I>>(base?: I): PriorContextChanged {
    return PriorContextChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PriorContextChanged>, I>>(object: I): PriorContextChanged {
    const message = createBasePriorContextChanged();
    message.summary = (object.summary !== undefined && object.summary !== null)
      ? PriorContextSummary.fromPartial(object.summary)
      : undefined;
    return message;
  },
};

function createBaseItemsUpdate(): ItemsUpdate {
  return { mode: "", items: [] };
}

export const ItemsUpdate: MessageFns<ItemsUpdate> = {
  encode(message: ItemsUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== "") {
      writer.uint32(10).string(message.mode);
    }
    for (const v of message.items) {
      Item.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemsUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItemsUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ItemsUpdate {
    return {
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: ItemsUpdate): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemsUpdate>, I>>(base?: I): ItemsUpdate {
    return ItemsUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemsUpdate>, I>>(object: I): ItemsUpdate {
    const message = createBaseItemsUpdate();
    message.mode = object.mode ?? "";
    message.items = object.items?.map((e) => Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseItemUpdated(): ItemUpdated {
  return { mode: "", item: undefined };
}

export const ItemUpdated: MessageFns<ItemUpdated> = {
  encode(message: ItemUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== "") {
      writer.uint32(10).string(message.mode);
    }
    if (message.item !== undefined) {
      Item.encode(message.item, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItemUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.item = Item.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ItemUpdated {
    return {
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      item: isSet(object.item) ? Item.fromJSON(object.item) : undefined,
    };
  },

  toJSON(message: ItemUpdated): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.item !== undefined) {
      obj.item = Item.toJSON(message.item);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemUpdated>, I>>(base?: I): ItemUpdated {
    return ItemUpdated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemUpdated>, I>>(object: I): ItemUpdated {
    const message = createBaseItemUpdated();
    message.mode = object.mode ?? "";
    message.item = (object.item !== undefined && object.item !== null) ? Item.fromPartial(object.item) : undefined;
    return message;
  },
};

function createBaseTranscriptInterim(): TranscriptInterim {
  return { text: "" };
}

export const TranscriptInterim: MessageFns<TranscriptInterim> = {
  encode(message: TranscriptInterim, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptInterim {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptInterim();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptInterim {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: TranscriptInterim): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscriptInterim>, I>>(base?: I): TranscriptInterim {
    return TranscriptInterim.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscriptInterim>, I>>(object: I): TranscriptInterim {
    const message = createBaseTranscriptInterim();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseStatusEvent(): StatusEvent {
  return { status: undefined };
}

export const StatusEvent: MessageFns<StatusEvent> = {
  encode(message: StatusEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      Status.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = Status.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusEvent {
    return { status: isSet(object.status) ? Status.fromJSON(object.status) : undefined };
  },

  toJSON(message: StatusEvent): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = Status.toJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusEvent>, I>>(base?: I): StatusEvent {
    return StatusEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusEvent>, I>>(object: I): StatusEvent {
    const message = createBaseStatusEvent();
    message.status = (object.status !== undefined && object.status !== null)
      ? Status.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseError(): Error {
  return { code: "", message: "", intentRef: undefined };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.intentRef !== undefined) {
      writer.uint32(26).string(message.intentRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.intentRef = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Error {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      intentRef: isSet(object.intentRef)
        ? globalThis.String(object.intentRef)
        : isSet(object.intent_ref)
        ? globalThis.String(object.intent_ref)
        : undefined,
    };
  },

  toJSON(message: Error): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.intentRef !== undefined) {
      obj.intentRef = message.intentRef;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Error>, I>>(base?: I): Error {
    return Error.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Error>, I>>(object: I): Error {
    const message = createBaseError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.intentRef = object.intentRef ?? undefined;
    return message;
  },
};

function createBaseDeviceRegistered(): DeviceRegistered {
  return { device: undefined };
}

export const DeviceRegistered: MessageFns<DeviceRegistered> = {
  encode(message: DeviceRegistered, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.device !== undefined) {
      Device.encode(message.device, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceRegistered {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceRegistered();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.device = Device.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceRegistered {
    return { device: isSet(object.device) ? Device.fromJSON(object.device) : undefined };
  },

  toJSON(message: DeviceRegistered): unknown {
    const obj: any = {};
    if (message.device !== undefined) {
      obj.device = Device.toJSON(message.device);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceRegistered>, I>>(base?: I): DeviceRegistered {
    return DeviceRegistered.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceRegistered>, I>>(object: I): DeviceRegistered {
    const message = createBaseDeviceRegistered();
    message.device = (object.device !== undefined && object.device !== null)
      ? Device.fromPartial(object.device)
      : undefined;
    return message;
  },
};

function createBaseDevicesChanged(): DevicesChanged {
  return { devices: [] };
}

export const DevicesChanged: MessageFns<DevicesChanged> = {
  encode(message: DevicesChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.devices) {
      Device.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DevicesChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevicesChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.devices.push(Device.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DevicesChanged {
    return {
      devices: globalThis.Array.isArray(object?.devices) ? object.devices.map((e: any) => Device.fromJSON(e)) : [],
    };
  },

  toJSON(message: DevicesChanged): unknown {
    const obj: any = {};
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => Device.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DevicesChanged>, I>>(base?: I): DevicesChanged {
    return DevicesChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DevicesChanged>, I>>(object: I): DevicesChanged {
    const message = createBaseDevicesChanged();
    message.devices = object.devices?.map((e) => Device.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAudioSourceDeviceChanged(): AudioSourceDeviceChanged {
  return { deviceId: undefined };
}

export const AudioSourceDeviceChanged: MessageFns<AudioSourceDeviceChanged> = {
  encode(message: AudioSourceDeviceChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      writer.uint32(10).string(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioSourceDeviceChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioSourceDeviceChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioSourceDeviceChanged {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : undefined,
    };
  },

  toJSON(message: AudioSourceDeviceChanged): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = message.deviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AudioSourceDeviceChanged>, I>>(base?: I): AudioSourceDeviceChanged {
    return AudioSourceDeviceChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AudioSourceDeviceChanged>, I>>(object: I): AudioSourceDeviceChanged {
    const message = createBaseAudioSourceDeviceChanged();
    message.deviceId = object.deviceId ?? undefined;
    return message;
  },
};

function createBaseArtifactsChanged(): ArtifactsChanged {
  return { artifactIds: [] };
}

export const ArtifactsChanged: MessageFns<ArtifactsChanged> = {
  encode(message: ArtifactsChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.artifactIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactsChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactsChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.artifactIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArtifactsChanged {
    return {
      artifactIds: globalThis.Array.isArray(object?.artifactIds)
        ? object.artifactIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.artifact_ids)
        ? object.artifact_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ArtifactsChanged): unknown {
    const obj: any = {};
    if (message.artifactIds?.length) {
      obj.artifactIds = message.artifactIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArtifactsChanged>, I>>(base?: I): ArtifactsChanged {
    return ArtifactsChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactsChanged>, I>>(object: I): ArtifactsChanged {
    const message = createBaseArtifactsChanged();
    message.artifactIds = object.artifactIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseCaptureMomentScreenshot(): CaptureMomentScreenshot {
  return { meetingId: "", momentId: "", tMs: 0 };
}

export const CaptureMomentScreenshot: MessageFns<CaptureMomentScreenshot> = {
  encode(message: CaptureMomentScreenshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meetingId !== "") {
      writer.uint32(10).string(message.meetingId);
    }
    if (message.momentId !== "") {
      writer.uint32(18).string(message.momentId);
    }
    if (message.tMs !== 0) {
      writer.uint32(24).int64(message.tMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureMomentScreenshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCaptureMomentScreenshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meetingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.momentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CaptureMomentScreenshot {
    return {
      meetingId: isSet(object.meetingId)
        ? globalThis.String(object.meetingId)
        : isSet(object.meeting_id)
        ? globalThis.String(object.meeting_id)
        : "",
      momentId: isSet(object.momentId)
        ? globalThis.String(object.momentId)
        : isSet(object.moment_id)
        ? globalThis.String(object.moment_id)
        : "",
      tMs: isSet(object.tMs) ? globalThis.Number(object.tMs) : isSet(object.t_ms) ? globalThis.Number(object.t_ms) : 0,
    };
  },

  toJSON(message: CaptureMomentScreenshot): unknown {
    const obj: any = {};
    if (message.meetingId !== "") {
      obj.meetingId = message.meetingId;
    }
    if (message.momentId !== "") {
      obj.momentId = message.momentId;
    }
    if (message.tMs !== 0) {
      obj.tMs = Math.round(message.tMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureMomentScreenshot>, I>>(base?: I): CaptureMomentScreenshot {
    return CaptureMomentScreenshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureMomentScreenshot>, I>>(object: I): CaptureMomentScreenshot {
    const message = createBaseCaptureMomentScreenshot();
    message.meetingId = object.meetingId ?? "";
    message.momentId = object.momentId ?? "";
    message.tMs = object.tMs ?? 0;
    return message;
  },
};

function createBaseMomentSummarized(): MomentSummarized {
  return { momentId: "", meetingId: "", tMs: 0, summary: "", note: undefined };
}

export const MomentSummarized: MessageFns<MomentSummarized> = {
  encode(message: MomentSummarized, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.momentId !== "") {
      writer.uint32(10).string(message.momentId);
    }
    if (message.meetingId !== "") {
      writer.uint32(18).string(message.meetingId);
    }
    if (message.tMs !== 0) {
      writer.uint32(24).int64(message.tMs);
    }
    if (message.summary !== "") {
      writer.uint32(34).string(message.summary);
    }
    if (message.note !== undefined) {
      writer.uint32(42).string(message.note);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MomentSummarized {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMomentSummarized();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.momentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meetingId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.note = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MomentSummarized {
    return {
      momentId: isSet(object.momentId)
        ? globalThis.String(object.momentId)
        : isSet(object.moment_id)
        ? globalThis.String(object.moment_id)
        : "",
      meetingId: isSet(object.meetingId)
        ? globalThis.String(object.meetingId)
        : isSet(object.meeting_id)
        ? globalThis.String(object.meeting_id)
        : "",
      tMs: isSet(object.tMs) ? globalThis.Number(object.tMs) : isSet(object.t_ms) ? globalThis.Number(object.t_ms) : 0,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      note: isSet(object.note) ? globalThis.String(object.note) : undefined,
    };
  },

  toJSON(message: MomentSummarized): unknown {
    const obj: any = {};
    if (message.momentId !== "") {
      obj.momentId = message.momentId;
    }
    if (message.meetingId !== "") {
      obj.meetingId = message.meetingId;
    }
    if (message.tMs !== 0) {
      obj.tMs = Math.round(message.tMs);
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.note !== undefined) {
      obj.note = message.note;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MomentSummarized>, I>>(base?: I): MomentSummarized {
    return MomentSummarized.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MomentSummarized>, I>>(object: I): MomentSummarized {
    const message = createBaseMomentSummarized();
    message.momentId = object.momentId ?? "";
    message.meetingId = object.meetingId ?? "";
    message.tMs = object.tMs ?? 0;
    message.summary = object.summary ?? "";
    message.note = object.note ?? undefined;
    return message;
  },
};

function createBaseEvent(): Event {
  return { kind: undefined };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "snapshot":
        Snapshot.encode(message.kind.snapshot, writer.uint32(10).fork()).join();
        break;
      case "meetingStateChanged":
        MeetingStateChanged.encode(message.kind.meetingStateChanged, writer.uint32(18).fork()).join();
        break;
      case "modeChanged":
        ModeChanged.encode(message.kind.modeChanged, writer.uint32(26).fork()).join();
        break;
      case "displayTagChanged":
        DisplayTagChanged.encode(message.kind.displayTagChanged, writer.uint32(34).fork()).join();
        break;
      case "metadataChanged":
        MetadataChanged.encode(message.kind.metadataChanged, writer.uint32(42).fork()).join();
        break;
      case "priorContextChanged":
        PriorContextChanged.encode(message.kind.priorContextChanged, writer.uint32(50).fork()).join();
        break;
      case "itemsUpdate":
        ItemsUpdate.encode(message.kind.itemsUpdate, writer.uint32(58).fork()).join();
        break;
      case "itemUpdated":
        ItemUpdated.encode(message.kind.itemUpdated, writer.uint32(66).fork()).join();
        break;
      case "transcriptInterim":
        TranscriptInterim.encode(message.kind.transcriptInterim, writer.uint32(74).fork()).join();
        break;
      case "status":
        StatusEvent.encode(message.kind.status, writer.uint32(82).fork()).join();
        break;
      case "error":
        Error.encode(message.kind.error, writer.uint32(90).fork()).join();
        break;
      case "deviceRegistered":
        DeviceRegistered.encode(message.kind.deviceRegistered, writer.uint32(98).fork()).join();
        break;
      case "devicesChanged":
        DevicesChanged.encode(message.kind.devicesChanged, writer.uint32(106).fork()).join();
        break;
      case "audioSourceDeviceChanged":
        AudioSourceDeviceChanged.encode(message.kind.audioSourceDeviceChanged, writer.uint32(114).fork()).join();
        break;
      case "artifactsChanged":
        ArtifactsChanged.encode(message.kind.artifactsChanged, writer.uint32(122).fork()).join();
        break;
      case "captureMomentScreenshot":
        CaptureMomentScreenshot.encode(message.kind.captureMomentScreenshot, writer.uint32(130).fork()).join();
        break;
      case "momentSummarized":
        MomentSummarized.encode(message.kind.momentSummarized, writer.uint32(138).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "snapshot", snapshot: Snapshot.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = {
            $case: "meetingStateChanged",
            meetingStateChanged: MeetingStateChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = { $case: "modeChanged", modeChanged: ModeChanged.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kind = {
            $case: "displayTagChanged",
            displayTagChanged: DisplayTagChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.kind = { $case: "metadataChanged", metadataChanged: MetadataChanged.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = {
            $case: "priorContextChanged",
            priorContextChanged: PriorContextChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.kind = { $case: "itemsUpdate", itemsUpdate: ItemsUpdate.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.kind = { $case: "itemUpdated", itemUpdated: ItemUpdated.decode(reader, reader.uint32()) };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.kind = {
            $case: "transcriptInterim",
            transcriptInterim: TranscriptInterim.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.kind = { $case: "status", status: StatusEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.kind = { $case: "error", error: Error.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.kind = {
            $case: "deviceRegistered",
            deviceRegistered: DeviceRegistered.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.kind = { $case: "devicesChanged", devicesChanged: DevicesChanged.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.kind = {
            $case: "audioSourceDeviceChanged",
            audioSourceDeviceChanged: AudioSourceDeviceChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.kind = {
            $case: "artifactsChanged",
            artifactsChanged: ArtifactsChanged.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.kind = {
            $case: "captureMomentScreenshot",
            captureMomentScreenshot: CaptureMomentScreenshot.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.kind = {
            $case: "momentSummarized",
            momentSummarized: MomentSummarized.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      kind: isSet(object.snapshot)
        ? { $case: "snapshot", snapshot: Snapshot.fromJSON(object.snapshot) }
        : isSet(object.meetingStateChanged)
        ? {
          $case: "meetingStateChanged",
          meetingStateChanged: MeetingStateChanged.fromJSON(object.meetingStateChanged),
        }
        : isSet(object.meeting_state_changed)
        ? {
          $case: "meetingStateChanged",
          meetingStateChanged: MeetingStateChanged.fromJSON(object.meeting_state_changed),
        }
        : isSet(object.modeChanged)
        ? { $case: "modeChanged", modeChanged: ModeChanged.fromJSON(object.modeChanged) }
        : isSet(object.mode_changed)
        ? { $case: "modeChanged", modeChanged: ModeChanged.fromJSON(object.mode_changed) }
        : isSet(object.displayTagChanged)
        ? { $case: "displayTagChanged", displayTagChanged: DisplayTagChanged.fromJSON(object.displayTagChanged) }
        : isSet(object.display_tag_changed)
        ? { $case: "displayTagChanged", displayTagChanged: DisplayTagChanged.fromJSON(object.display_tag_changed) }
        : isSet(object.metadataChanged)
        ? { $case: "metadataChanged", metadataChanged: MetadataChanged.fromJSON(object.metadataChanged) }
        : isSet(object.metadata_changed)
        ? { $case: "metadataChanged", metadataChanged: MetadataChanged.fromJSON(object.metadata_changed) }
        : isSet(object.priorContextChanged)
        ? {
          $case: "priorContextChanged",
          priorContextChanged: PriorContextChanged.fromJSON(object.priorContextChanged),
        }
        : isSet(object.prior_context_changed)
        ? {
          $case: "priorContextChanged",
          priorContextChanged: PriorContextChanged.fromJSON(object.prior_context_changed),
        }
        : isSet(object.itemsUpdate)
        ? { $case: "itemsUpdate", itemsUpdate: ItemsUpdate.fromJSON(object.itemsUpdate) }
        : isSet(object.items_update)
        ? { $case: "itemsUpdate", itemsUpdate: ItemsUpdate.fromJSON(object.items_update) }
        : isSet(object.itemUpdated)
        ? { $case: "itemUpdated", itemUpdated: ItemUpdated.fromJSON(object.itemUpdated) }
        : isSet(object.item_updated)
        ? { $case: "itemUpdated", itemUpdated: ItemUpdated.fromJSON(object.item_updated) }
        : isSet(object.transcriptInterim)
        ? { $case: "transcriptInterim", transcriptInterim: TranscriptInterim.fromJSON(object.transcriptInterim) }
        : isSet(object.transcript_interim)
        ? { $case: "transcriptInterim", transcriptInterim: TranscriptInterim.fromJSON(object.transcript_interim) }
        : isSet(object.status)
        ? { $case: "status", status: StatusEvent.fromJSON(object.status) }
        : isSet(object.error)
        ? { $case: "error", error: Error.fromJSON(object.error) }
        : isSet(object.deviceRegistered)
        ? { $case: "deviceRegistered", deviceRegistered: DeviceRegistered.fromJSON(object.deviceRegistered) }
        : isSet(object.device_registered)
        ? { $case: "deviceRegistered", deviceRegistered: DeviceRegistered.fromJSON(object.device_registered) }
        : isSet(object.devicesChanged)
        ? { $case: "devicesChanged", devicesChanged: DevicesChanged.fromJSON(object.devicesChanged) }
        : isSet(object.devices_changed)
        ? { $case: "devicesChanged", devicesChanged: DevicesChanged.fromJSON(object.devices_changed) }
        : isSet(object.audioSourceDeviceChanged)
        ? {
          $case: "audioSourceDeviceChanged",
          audioSourceDeviceChanged: AudioSourceDeviceChanged.fromJSON(object.audioSourceDeviceChanged),
        }
        : isSet(object.audio_source_device_changed)
        ? {
          $case: "audioSourceDeviceChanged",
          audioSourceDeviceChanged: AudioSourceDeviceChanged.fromJSON(object.audio_source_device_changed),
        }
        : isSet(object.artifactsChanged)
        ? { $case: "artifactsChanged", artifactsChanged: ArtifactsChanged.fromJSON(object.artifactsChanged) }
        : isSet(object.artifacts_changed)
        ? { $case: "artifactsChanged", artifactsChanged: ArtifactsChanged.fromJSON(object.artifacts_changed) }
        : isSet(object.captureMomentScreenshot)
        ? {
          $case: "captureMomentScreenshot",
          captureMomentScreenshot: CaptureMomentScreenshot.fromJSON(object.captureMomentScreenshot),
        }
        : isSet(object.capture_moment_screenshot)
        ? {
          $case: "captureMomentScreenshot",
          captureMomentScreenshot: CaptureMomentScreenshot.fromJSON(object.capture_moment_screenshot),
        }
        : isSet(object.momentSummarized)
        ? { $case: "momentSummarized", momentSummarized: MomentSummarized.fromJSON(object.momentSummarized) }
        : isSet(object.moment_summarized)
        ? { $case: "momentSummarized", momentSummarized: MomentSummarized.fromJSON(object.moment_summarized) }
        : undefined,
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.kind?.$case === "snapshot") {
      obj.snapshot = Snapshot.toJSON(message.kind.snapshot);
    } else if (message.kind?.$case === "meetingStateChanged") {
      obj.meetingStateChanged = MeetingStateChanged.toJSON(message.kind.meetingStateChanged);
    } else if (message.kind?.$case === "modeChanged") {
      obj.modeChanged = ModeChanged.toJSON(message.kind.modeChanged);
    } else if (message.kind?.$case === "displayTagChanged") {
      obj.displayTagChanged = DisplayTagChanged.toJSON(message.kind.displayTagChanged);
    } else if (message.kind?.$case === "metadataChanged") {
      obj.metadataChanged = MetadataChanged.toJSON(message.kind.metadataChanged);
    } else if (message.kind?.$case === "priorContextChanged") {
      obj.priorContextChanged = PriorContextChanged.toJSON(message.kind.priorContextChanged);
    } else if (message.kind?.$case === "itemsUpdate") {
      obj.itemsUpdate = ItemsUpdate.toJSON(message.kind.itemsUpdate);
    } else if (message.kind?.$case === "itemUpdated") {
      obj.itemUpdated = ItemUpdated.toJSON(message.kind.itemUpdated);
    } else if (message.kind?.$case === "transcriptInterim") {
      obj.transcriptInterim = TranscriptInterim.toJSON(message.kind.transcriptInterim);
    } else if (message.kind?.$case === "status") {
      obj.status = StatusEvent.toJSON(message.kind.status);
    } else if (message.kind?.$case === "error") {
      obj.error = Error.toJSON(message.kind.error);
    } else if (message.kind?.$case === "deviceRegistered") {
      obj.deviceRegistered = DeviceRegistered.toJSON(message.kind.deviceRegistered);
    } else if (message.kind?.$case === "devicesChanged") {
      obj.devicesChanged = DevicesChanged.toJSON(message.kind.devicesChanged);
    } else if (message.kind?.$case === "audioSourceDeviceChanged") {
      obj.audioSourceDeviceChanged = AudioSourceDeviceChanged.toJSON(message.kind.audioSourceDeviceChanged);
    } else if (message.kind?.$case === "artifactsChanged") {
      obj.artifactsChanged = ArtifactsChanged.toJSON(message.kind.artifactsChanged);
    } else if (message.kind?.$case === "captureMomentScreenshot") {
      obj.captureMomentScreenshot = CaptureMomentScreenshot.toJSON(message.kind.captureMomentScreenshot);
    } else if (message.kind?.$case === "momentSummarized") {
      obj.momentSummarized = MomentSummarized.toJSON(message.kind.momentSummarized);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    switch (object.kind?.$case) {
      case "snapshot": {
        if (object.kind?.snapshot !== undefined && object.kind?.snapshot !== null) {
          message.kind = { $case: "snapshot", snapshot: Snapshot.fromPartial(object.kind.snapshot) };
        }
        break;
      }
      case "meetingStateChanged": {
        if (object.kind?.meetingStateChanged !== undefined && object.kind?.meetingStateChanged !== null) {
          message.kind = {
            $case: "meetingStateChanged",
            meetingStateChanged: MeetingStateChanged.fromPartial(object.kind.meetingStateChanged),
          };
        }
        break;
      }
      case "modeChanged": {
        if (object.kind?.modeChanged !== undefined && object.kind?.modeChanged !== null) {
          message.kind = { $case: "modeChanged", modeChanged: ModeChanged.fromPartial(object.kind.modeChanged) };
        }
        break;
      }
      case "displayTagChanged": {
        if (object.kind?.displayTagChanged !== undefined && object.kind?.displayTagChanged !== null) {
          message.kind = {
            $case: "displayTagChanged",
            displayTagChanged: DisplayTagChanged.fromPartial(object.kind.displayTagChanged),
          };
        }
        break;
      }
      case "metadataChanged": {
        if (object.kind?.metadataChanged !== undefined && object.kind?.metadataChanged !== null) {
          message.kind = {
            $case: "metadataChanged",
            metadataChanged: MetadataChanged.fromPartial(object.kind.metadataChanged),
          };
        }
        break;
      }
      case "priorContextChanged": {
        if (object.kind?.priorContextChanged !== undefined && object.kind?.priorContextChanged !== null) {
          message.kind = {
            $case: "priorContextChanged",
            priorContextChanged: PriorContextChanged.fromPartial(object.kind.priorContextChanged),
          };
        }
        break;
      }
      case "itemsUpdate": {
        if (object.kind?.itemsUpdate !== undefined && object.kind?.itemsUpdate !== null) {
          message.kind = { $case: "itemsUpdate", itemsUpdate: ItemsUpdate.fromPartial(object.kind.itemsUpdate) };
        }
        break;
      }
      case "itemUpdated": {
        if (object.kind?.itemUpdated !== undefined && object.kind?.itemUpdated !== null) {
          message.kind = { $case: "itemUpdated", itemUpdated: ItemUpdated.fromPartial(object.kind.itemUpdated) };
        }
        break;
      }
      case "transcriptInterim": {
        if (object.kind?.transcriptInterim !== undefined && object.kind?.transcriptInterim !== null) {
          message.kind = {
            $case: "transcriptInterim",
            transcriptInterim: TranscriptInterim.fromPartial(object.kind.transcriptInterim),
          };
        }
        break;
      }
      case "status": {
        if (object.kind?.status !== undefined && object.kind?.status !== null) {
          message.kind = { $case: "status", status: StatusEvent.fromPartial(object.kind.status) };
        }
        break;
      }
      case "error": {
        if (object.kind?.error !== undefined && object.kind?.error !== null) {
          message.kind = { $case: "error", error: Error.fromPartial(object.kind.error) };
        }
        break;
      }
      case "deviceRegistered": {
        if (object.kind?.deviceRegistered !== undefined && object.kind?.deviceRegistered !== null) {
          message.kind = {
            $case: "deviceRegistered",
            deviceRegistered: DeviceRegistered.fromPartial(object.kind.deviceRegistered),
          };
        }
        break;
      }
      case "devicesChanged": {
        if (object.kind?.devicesChanged !== undefined && object.kind?.devicesChanged !== null) {
          message.kind = {
            $case: "devicesChanged",
            devicesChanged: DevicesChanged.fromPartial(object.kind.devicesChanged),
          };
        }
        break;
      }
      case "audioSourceDeviceChanged": {
        if (object.kind?.audioSourceDeviceChanged !== undefined && object.kind?.audioSourceDeviceChanged !== null) {
          message.kind = {
            $case: "audioSourceDeviceChanged",
            audioSourceDeviceChanged: AudioSourceDeviceChanged.fromPartial(object.kind.audioSourceDeviceChanged),
          };
        }
        break;
      }
      case "artifactsChanged": {
        if (object.kind?.artifactsChanged !== undefined && object.kind?.artifactsChanged !== null) {
          message.kind = {
            $case: "artifactsChanged",
            artifactsChanged: ArtifactsChanged.fromPartial(object.kind.artifactsChanged),
          };
        }
        break;
      }
      case "captureMomentScreenshot": {
        if (object.kind?.captureMomentScreenshot !== undefined && object.kind?.captureMomentScreenshot !== null) {
          message.kind = {
            $case: "captureMomentScreenshot",
            captureMomentScreenshot: CaptureMomentScreenshot.fromPartial(object.kind.captureMomentScreenshot),
          };
        }
        break;
      }
      case "momentSummarized": {
        if (object.kind?.momentSummarized !== undefined && object.kind?.momentSummarized !== null) {
          message.kind = {
            $case: "momentSummarized",
            momentSummarized: MomentSummarized.fromPartial(object.kind.momentSummarized),
          };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: meeting_companion/v1/intents.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Capability, capabilityFromJSON, capabilityToJSON } from "./common";

export const protobufPackage = "meeting_companion.v1";

/**
 * Idle → Active. If `description` is present and `metadata` empty,
 * server fires LLM extraction in background.
 * `audio_source_device_id` binds a registered device's `/audio`
 * stream as the meeting's audio source.
 */
export interface StartMeeting {
  description?: string | undefined;
  metadata: { [key: string]: string };
  audioSourceDeviceId?: string | undefined;
}

export interface StartMeeting_MetadataEntry {
  key: string;
  value: string;
}

/**
 * Active/Paused → Idle. Clears items, metadata, recalled context,
 * in-flight extraction. Pushes the summary bundle to mnemo.
 */
export interface StopMeeting {}

/** Active → Paused. Audio + STT continue but the agent idles. */
export interface Pause {}

/** Paused → Active. */
export interface Resume {}

/** Switch the current mode. Mode tab clicks on any client. */
export interface SetMode {
  mode: string;
}

/**
 * Insert / replace / delete one metadata key. `null` value (i.e.
 * `value` field unset) deletes the key. Always emits
 * `metadata_changed`.
 */
export interface SetMetadata {
  key: string;
  value?: string | undefined;
}

/**
 * Trigger LLM extraction in idle without changing state. Result
 * merges with existing metadata via "manual wins". See ADR-0010.
 */
export interface ExtractMetadata {
  description: string;
}

/**
 * Capability-bearing client (Mac, future glasses) declares itself.
 * Server replies `device_registered` to the sender and
 * `devices_changed` to the user's other clients.
 */
export interface RegisterDevice {
  hostname: string;
  capabilities: Capability[];
}

/**
 * Insert a moment row at timestamp `t` (ms since meeting start).
 * If a `screen_capture`-capable device is bound, server sends it
 * `capture_moment_screenshot`; the device uploads via REST. Vision-LLM
 * summarizer fires on the upload.
 */
export interface MarkMoment {
  t: number;
  note?: string | undefined;
}

/**
 * Asks the agent to fill the item's `detail` field. Agent emits
 * `item_updated` for the current mode with the enriched item.
 */
export interface ExpandItem {
  itemId: string;
}

/**
 * User question for the agent during an active meeting. Renders as
 * a Q+A bubble pair in chat mode (Replace strategy, single pair —
 * each new question replaces the previous exchange).
 */
export interface Chat {
  text: string;
}

/**
 * Top-level intent envelope. `kind` carries exactly one variant;
 * the server's dispatcher pattern-matches on which variant is set.
 */
export interface Intent {
  kind?:
    | { $case: "startMeeting"; startMeeting: StartMeeting }
    | { $case: "stopMeeting"; stopMeeting: StopMeeting }
    | { $case: "pause"; pause: Pause }
    | { $case: "resume"; resume: Resume }
    | { $case: "setMode"; setMode: SetMode }
    | { $case: "setMetadata"; setMetadata: SetMetadata }
    | { $case: "extractMetadata"; extractMetadata: ExtractMetadata }
    | { $case: "registerDevice"; registerDevice: RegisterDevice }
    | { $case: "markMoment"; markMoment: MarkMoment }
    | { $case: "expandItem"; expandItem: ExpandItem }
    | { $case: "chat"; chat: Chat }
    | undefined;
}

function createBaseStartMeeting(): StartMeeting {
  return { description: undefined, metadata: {}, audioSourceDeviceId: undefined };
}

export const StartMeeting: MessageFns<StartMeeting> = {
  encode(message: StartMeeting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== undefined) {
      writer.uint32(10).string(message.description);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      StartMeeting_MetadataEntry.encode(
        { key: key as any, value },
        writer.uint32(18).fork(),
      ).join();
    });
    if (message.audioSourceDeviceId !== undefined) {
      writer.uint32(26).string(message.audioSourceDeviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartMeeting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartMeeting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = StartMeeting_MetadataEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.metadata[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.audioSourceDeviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartMeeting {
    return {
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
            (acc: { [key: string]: string }, [key, value]: [string, any]) => {
              acc[key] = globalThis.String(value);
              return acc;
            },
            {},
          )
        : {},
      audioSourceDeviceId: isSet(object.audioSourceDeviceId)
        ? globalThis.String(object.audioSourceDeviceId)
        : isSet(object.audio_source_device_id)
          ? globalThis.String(object.audio_source_device_id)
          : undefined,
    };
  },

  toJSON(message: StartMeeting): unknown {
    const obj: any = {};
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.audioSourceDeviceId !== undefined) {
      obj.audioSourceDeviceId = message.audioSourceDeviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartMeeting>, I>>(base?: I): StartMeeting {
    return StartMeeting.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartMeeting>, I>>(object: I): StartMeeting {
    const message = createBaseStartMeeting();
    message.description = object.description ?? undefined;
    message.metadata = (
      globalThis.Object.entries(object.metadata ?? {}) as [string, string][]
    ).reduce((acc: { [key: string]: string }, [key, value]: [string, string]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.audioSourceDeviceId = object.audioSourceDeviceId ?? undefined;
    return message;
  },
};

function createBaseStartMeeting_MetadataEntry(): StartMeeting_MetadataEntry {
  return { key: "", value: "" };
}

export const StartMeeting_MetadataEntry: MessageFns<StartMeeting_MetadataEntry> = {
  encode(
    message: StartMeeting_MetadataEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartMeeting_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartMeeting_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartMeeting_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: StartMeeting_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartMeeting_MetadataEntry>, I>>(
    base?: I,
  ): StartMeeting_MetadataEntry {
    return StartMeeting_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartMeeting_MetadataEntry>, I>>(
    object: I,
  ): StartMeeting_MetadataEntry {
    const message = createBaseStartMeeting_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseStopMeeting(): StopMeeting {
  return {};
}

export const StopMeeting: MessageFns<StopMeeting> = {
  encode(_: StopMeeting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopMeeting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopMeeting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StopMeeting {
    return {};
  },

  toJSON(_: StopMeeting): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StopMeeting>, I>>(base?: I): StopMeeting {
    return StopMeeting.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopMeeting>, I>>(_: I): StopMeeting {
    const message = createBaseStopMeeting();
    return message;
  },
};

function createBasePause(): Pause {
  return {};
}

export const Pause: MessageFns<Pause> = {
  encode(_: Pause, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Pause {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePause();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Pause {
    return {};
  },

  toJSON(_: Pause): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Pause>, I>>(base?: I): Pause {
    return Pause.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Pause>, I>>(_: I): Pause {
    const message = createBasePause();
    return message;
  },
};

function createBaseResume(): Resume {
  return {};
}

export const Resume: MessageFns<Resume> = {
  encode(_: Resume, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Resume {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResume();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Resume {
    return {};
  },

  toJSON(_: Resume): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Resume>, I>>(base?: I): Resume {
    return Resume.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Resume>, I>>(_: I): Resume {
    const message = createBaseResume();
    return message;
  },
};

function createBaseSetMode(): SetMode {
  return { mode: "" };
}

export const SetMode: MessageFns<SetMode> = {
  encode(message: SetMode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== "") {
      writer.uint32(10).string(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMode {
    return { mode: isSet(object.mode) ? globalThis.String(object.mode) : "" };
  },

  toJSON(message: SetMode): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMode>, I>>(base?: I): SetMode {
    return SetMode.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMode>, I>>(object: I): SetMode {
    const message = createBaseSetMode();
    message.mode = object.mode ?? "";
    return message;
  },
};

function createBaseSetMetadata(): SetMetadata {
  return { key: "", value: undefined };
}

export const SetMetadata: MessageFns<SetMetadata> = {
  encode(message: SetMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMetadata {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : undefined,
    };
  },

  toJSON(message: SetMetadata): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMetadata>, I>>(base?: I): SetMetadata {
    return SetMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMetadata>, I>>(object: I): SetMetadata {
    const message = createBaseSetMetadata();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseExtractMetadata(): ExtractMetadata {
  return { description: "" };
}

export const ExtractMetadata: MessageFns<ExtractMetadata> = {
  encode(message: ExtractMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtractMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtractMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExtractMetadata {
    return { description: isSet(object.description) ? globalThis.String(object.description) : "" };
  },

  toJSON(message: ExtractMetadata): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExtractMetadata>, I>>(base?: I): ExtractMetadata {
    return ExtractMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExtractMetadata>, I>>(object: I): ExtractMetadata {
    const message = createBaseExtractMetadata();
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseRegisterDevice(): RegisterDevice {
  return { hostname: "", capabilities: [] };
}

export const RegisterDevice: MessageFns<RegisterDevice> = {
  encode(message: RegisterDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hostname !== "") {
      writer.uint32(10).string(message.hostname);
    }
    writer.uint32(18).fork();
    for (const v of message.capabilities) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.capabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.capabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDevice {
    return {
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => capabilityFromJSON(e))
        : [],
    };
  },

  toJSON(message: RegisterDevice): unknown {
    const obj: any = {};
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => capabilityToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDevice>, I>>(base?: I): RegisterDevice {
    return RegisterDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDevice>, I>>(object: I): RegisterDevice {
    const message = createBaseRegisterDevice();
    message.hostname = object.hostname ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    return message;
  },
};

function createBaseMarkMoment(): MarkMoment {
  return { t: 0, note: undefined };
}

export const MarkMoment: MessageFns<MarkMoment> = {
  encode(message: MarkMoment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.t !== 0) {
      writer.uint32(8).uint64(message.t);
    }
    if (message.note !== undefined) {
      writer.uint32(18).string(message.note);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarkMoment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkMoment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.t = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.note = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkMoment {
    return {
      t: isSet(object.t) ? globalThis.Number(object.t) : 0,
      note: isSet(object.note) ? globalThis.String(object.note) : undefined,
    };
  },

  toJSON(message: MarkMoment): unknown {
    const obj: any = {};
    if (message.t !== 0) {
      obj.t = Math.round(message.t);
    }
    if (message.note !== undefined) {
      obj.note = message.note;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarkMoment>, I>>(base?: I): MarkMoment {
    return MarkMoment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarkMoment>, I>>(object: I): MarkMoment {
    const message = createBaseMarkMoment();
    message.t = object.t ?? 0;
    message.note = object.note ?? undefined;
    return message;
  },
};

function createBaseExpandItem(): ExpandItem {
  return { itemId: "" };
}

export const ExpandItem: MessageFns<ExpandItem> = {
  encode(message: ExpandItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.itemId !== "") {
      writer.uint32(10).string(message.itemId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExpandItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpandItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.itemId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExpandItem {
    return {
      itemId: isSet(object.itemId)
        ? globalThis.String(object.itemId)
        : isSet(object.item_id)
          ? globalThis.String(object.item_id)
          : "",
    };
  },

  toJSON(message: ExpandItem): unknown {
    const obj: any = {};
    if (message.itemId !== "") {
      obj.itemId = message.itemId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExpandItem>, I>>(base?: I): ExpandItem {
    return ExpandItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExpandItem>, I>>(object: I): ExpandItem {
    const message = createBaseExpandItem();
    message.itemId = object.itemId ?? "";
    return message;
  },
};

function createBaseChat(): Chat {
  return { text: "" };
}

export const Chat: MessageFns<Chat> = {
  encode(message: Chat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Chat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Chat {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: Chat): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Chat>, I>>(base?: I): Chat {
    return Chat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Chat>, I>>(object: I): Chat {
    const message = createBaseChat();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseIntent(): Intent {
  return { kind: undefined };
}

export const Intent: MessageFns<Intent> = {
  encode(message: Intent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "startMeeting":
        StartMeeting.encode(message.kind.startMeeting, writer.uint32(10).fork()).join();
        break;
      case "stopMeeting":
        StopMeeting.encode(message.kind.stopMeeting, writer.uint32(18).fork()).join();
        break;
      case "pause":
        Pause.encode(message.kind.pause, writer.uint32(26).fork()).join();
        break;
      case "resume":
        Resume.encode(message.kind.resume, writer.uint32(34).fork()).join();
        break;
      case "setMode":
        SetMode.encode(message.kind.setMode, writer.uint32(42).fork()).join();
        break;
      case "setMetadata":
        SetMetadata.encode(message.kind.setMetadata, writer.uint32(50).fork()).join();
        break;
      case "extractMetadata":
        ExtractMetadata.encode(message.kind.extractMetadata, writer.uint32(58).fork()).join();
        break;
      case "registerDevice":
        RegisterDevice.encode(message.kind.registerDevice, writer.uint32(66).fork()).join();
        break;
      case "markMoment":
        MarkMoment.encode(message.kind.markMoment, writer.uint32(74).fork()).join();
        break;
      case "expandItem":
        ExpandItem.encode(message.kind.expandItem, writer.uint32(82).fork()).join();
        break;
      case "chat":
        Chat.encode(message.kind.chat, writer.uint32(90).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Intent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIntent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = {
            $case: "startMeeting",
            startMeeting: StartMeeting.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = {
            $case: "stopMeeting",
            stopMeeting: StopMeeting.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = { $case: "pause", pause: Pause.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kind = { $case: "resume", resume: Resume.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.kind = { $case: "setMode", setMode: SetMode.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = {
            $case: "setMetadata",
            setMetadata: SetMetadata.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.kind = {
            $case: "extractMetadata",
            extractMetadata: ExtractMetadata.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.kind = {
            $case: "registerDevice",
            registerDevice: RegisterDevice.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.kind = {
            $case: "markMoment",
            markMoment: MarkMoment.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.kind = {
            $case: "expandItem",
            expandItem: ExpandItem.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.kind = { $case: "chat", chat: Chat.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Intent {
    return {
      kind: isSet(object.startMeeting)
        ? { $case: "startMeeting", startMeeting: StartMeeting.fromJSON(object.startMeeting) }
        : isSet(object.start_meeting)
          ? { $case: "startMeeting", startMeeting: StartMeeting.fromJSON(object.start_meeting) }
          : isSet(object.stopMeeting)
            ? { $case: "stopMeeting", stopMeeting: StopMeeting.fromJSON(object.stopMeeting) }
            : isSet(object.stop_meeting)
              ? { $case: "stopMeeting", stopMeeting: StopMeeting.fromJSON(object.stop_meeting) }
              : isSet(object.pause)
                ? { $case: "pause", pause: Pause.fromJSON(object.pause) }
                : isSet(object.resume)
                  ? { $case: "resume", resume: Resume.fromJSON(object.resume) }
                  : isSet(object.setMode)
                    ? { $case: "setMode", setMode: SetMode.fromJSON(object.setMode) }
                    : isSet(object.set_mode)
                      ? { $case: "setMode", setMode: SetMode.fromJSON(object.set_mode) }
                      : isSet(object.setMetadata)
                        ? {
                            $case: "setMetadata",
                            setMetadata: SetMetadata.fromJSON(object.setMetadata),
                          }
                        : isSet(object.set_metadata)
                          ? {
                              $case: "setMetadata",
                              setMetadata: SetMetadata.fromJSON(object.set_metadata),
                            }
                          : isSet(object.extractMetadata)
                            ? {
                                $case: "extractMetadata",
                                extractMetadata: ExtractMetadata.fromJSON(object.extractMetadata),
                              }
                            : isSet(object.extract_metadata)
                              ? {
                                  $case: "extractMetadata",
                                  extractMetadata: ExtractMetadata.fromJSON(
                                    object.extract_metadata,
                                  ),
                                }
                              : isSet(object.registerDevice)
                                ? {
                                    $case: "registerDevice",
                                    registerDevice: RegisterDevice.fromJSON(object.registerDevice),
                                  }
                                : isSet(object.register_device)
                                  ? {
                                      $case: "registerDevice",
                                      registerDevice: RegisterDevice.fromJSON(
                                        object.register_device,
                                      ),
                                    }
                                  : isSet(object.markMoment)
                                    ? {
                                        $case: "markMoment",
                                        markMoment: MarkMoment.fromJSON(object.markMoment),
                                      }
                                    : isSet(object.mark_moment)
                                      ? {
                                          $case: "markMoment",
                                          markMoment: MarkMoment.fromJSON(object.mark_moment),
                                        }
                                      : isSet(object.expandItem)
                                        ? {
                                            $case: "expandItem",
                                            expandItem: ExpandItem.fromJSON(object.expandItem),
                                          }
                                        : isSet(object.expand_item)
                                          ? {
                                              $case: "expandItem",
                                              expandItem: ExpandItem.fromJSON(object.expand_item),
                                            }
                                          : isSet(object.chat)
                                            ? { $case: "chat", chat: Chat.fromJSON(object.chat) }
                                            : undefined,
    };
  },

  toJSON(message: Intent): unknown {
    const obj: any = {};
    if (message.kind?.$case === "startMeeting") {
      obj.startMeeting = StartMeeting.toJSON(message.kind.startMeeting);
    } else if (message.kind?.$case === "stopMeeting") {
      obj.stopMeeting = StopMeeting.toJSON(message.kind.stopMeeting);
    } else if (message.kind?.$case === "pause") {
      obj.pause = Pause.toJSON(message.kind.pause);
    } else if (message.kind?.$case === "resume") {
      obj.resume = Resume.toJSON(message.kind.resume);
    } else if (message.kind?.$case === "setMode") {
      obj.setMode = SetMode.toJSON(message.kind.setMode);
    } else if (message.kind?.$case === "setMetadata") {
      obj.setMetadata = SetMetadata.toJSON(message.kind.setMetadata);
    } else if (message.kind?.$case === "extractMetadata") {
      obj.extractMetadata = ExtractMetadata.toJSON(message.kind.extractMetadata);
    } else if (message.kind?.$case === "registerDevice") {
      obj.registerDevice = RegisterDevice.toJSON(message.kind.registerDevice);
    } else if (message.kind?.$case === "markMoment") {
      obj.markMoment = MarkMoment.toJSON(message.kind.markMoment);
    } else if (message.kind?.$case === "expandItem") {
      obj.expandItem = ExpandItem.toJSON(message.kind.expandItem);
    } else if (message.kind?.$case === "chat") {
      obj.chat = Chat.toJSON(message.kind.chat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Intent>, I>>(base?: I): Intent {
    return Intent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Intent>, I>>(object: I): Intent {
    const message = createBaseIntent();
    switch (object.kind?.$case) {
      case "startMeeting": {
        if (object.kind?.startMeeting !== undefined && object.kind?.startMeeting !== null) {
          message.kind = {
            $case: "startMeeting",
            startMeeting: StartMeeting.fromPartial(object.kind.startMeeting),
          };
        }
        break;
      }
      case "stopMeeting": {
        if (object.kind?.stopMeeting !== undefined && object.kind?.stopMeeting !== null) {
          message.kind = {
            $case: "stopMeeting",
            stopMeeting: StopMeeting.fromPartial(object.kind.stopMeeting),
          };
        }
        break;
      }
      case "pause": {
        if (object.kind?.pause !== undefined && object.kind?.pause !== null) {
          message.kind = { $case: "pause", pause: Pause.fromPartial(object.kind.pause) };
        }
        break;
      }
      case "resume": {
        if (object.kind?.resume !== undefined && object.kind?.resume !== null) {
          message.kind = { $case: "resume", resume: Resume.fromPartial(object.kind.resume) };
        }
        break;
      }
      case "setMode": {
        if (object.kind?.setMode !== undefined && object.kind?.setMode !== null) {
          message.kind = { $case: "setMode", setMode: SetMode.fromPartial(object.kind.setMode) };
        }
        break;
      }
      case "setMetadata": {
        if (object.kind?.setMetadata !== undefined && object.kind?.setMetadata !== null) {
          message.kind = {
            $case: "setMetadata",
            setMetadata: SetMetadata.fromPartial(object.kind.setMetadata),
          };
        }
        break;
      }
      case "extractMetadata": {
        if (object.kind?.extractMetadata !== undefined && object.kind?.extractMetadata !== null) {
          message.kind = {
            $case: "extractMetadata",
            extractMetadata: ExtractMetadata.fromPartial(object.kind.extractMetadata),
          };
        }
        break;
      }
      case "registerDevice": {
        if (object.kind?.registerDevice !== undefined && object.kind?.registerDevice !== null) {
          message.kind = {
            $case: "registerDevice",
            registerDevice: RegisterDevice.fromPartial(object.kind.registerDevice),
          };
        }
        break;
      }
      case "markMoment": {
        if (object.kind?.markMoment !== undefined && object.kind?.markMoment !== null) {
          message.kind = {
            $case: "markMoment",
            markMoment: MarkMoment.fromPartial(object.kind.markMoment),
          };
        }
        break;
      }
      case "expandItem": {
        if (object.kind?.expandItem !== undefined && object.kind?.expandItem !== null) {
          message.kind = {
            $case: "expandItem",
            expandItem: ExpandItem.fromPartial(object.kind.expandItem),
          };
        }
        break;
      }
      case "chat": {
        if (object.kind?.chat !== undefined && object.kind?.chat !== null) {
          message.kind = { $case: "chat", chat: Chat.fromPartial(object.kind.chat) };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends { $case: string }
        ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
